{
  "name": "finance-freedom-demo",
  "description": "Finance Freedom (\"Ledger\") — public cockpit demo. A personal-finance command center where every figure derives from one canonical register. Mock data; the engine is private.",
  "homepage": "https://garthpuckerin-finance-freedom.vercel.app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "@vitejs/plugin-react": "^4.2.1",
    "vite": "^5.0.8"
  }
}
