# Finance Freedom — environment variables
#
# This app is frontend-only and runs entirely on static mock fixtures.
# No environment variables are required to run the dev server or build.
#
# If you extend this with a real backend (auth, bank/account aggregation,
# persistence), add variables here (e.g. VITE_API_BASE_URL).
